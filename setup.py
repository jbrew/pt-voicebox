from setuptools import setup

setup(name='voicebox',
      version='0.2',
      description='Predictive text writing tool',
      url='',
      author='Jamie Brew',
      license='',
      install_requires=[
            'nose==1.3.7',
            'six==1.10.0'
      ],
      packages=['voicebox'])
